# Example environment variables for the AI Trader application

# --- Database Configuration ---
# Uncomment and set these variables if you are using PostgreSQL
# DB_TYPE=postgresql
# DB_USER=your_postgres_user
# DB_PASSWORD=your_postgres_password
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ai_trader_db

# If using SQLite, no specific env vars are strictly needed for basic connection,
# but you can define the path if you want it to be configurable:
# DB_TYPE=sqlite
# SQLITE_DB_PATH=./ai_trader.db

# --- API Keys (Example) ---
# Store sensitive API keys here and do not commit the actual .env file
# EXCHANGE_API_KEY="your_exchange_api_key"
# EXCHANGE_API_SECRET="your_exchange_api_secret"

# --- Other Configurations ---
# LOG_LEVEL=INFO
