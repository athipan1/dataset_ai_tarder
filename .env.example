# ==============================================================================
# Environment variables for AI Trader Project
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# SQLAlchemy Database URL. This is the connection string for your database.
#
# Option 1: SQLite (simplest, file-based)
# The path `./ai_trader.db` means the file `ai_trader.db` will be created
# in the root directory of the project (i.e., the same directory as this .env file,
# if the application is run from the project root).
DATABASE_URL="sqlite:///./ai_trader.db"

# Option 2: PostgreSQL (more robust, for production or complex development)
# Replace `user`, `password`, `host`, `port`, and `dbname` with your PostgreSQL details.
# DATABASE_URL="postgresql://user:password@host:port/dbname"

# Example for a local PostgreSQL server:
# Assuming user 'pguser', password 'pgpass', running on localhost default port 5432,
# with a database named 'aitrader':
# DATABASE_URL="postgresql://pguser:pgpass@localhost:5432/aitrader"

# ------------------------------------------------------------------------------
# Application Settings (Examples - customize as needed)
# ------------------------------------------------------------------------------
# DEBUG_MODE=True
# SECRET_KEY="your_very_secret_and_unique_key_here"
# LOG_LEVEL="INFO"

# ------------------------------------------------------------------------------
# API Keys (Examples - if your trader interacts with external services)
# ------------------------------------------------------------------------------
# EXCHANGE_API_KEY="YOUR_EXCHANGE_API_KEY"
# EXCHANGE_API_SECRET="YOUR_EXCHANGE_API_SECRET"

# ==============================================================================
# Instructions:
# 1. Rename this file to `.env`
# 2. Fill in the appropriate values for your environment.
#    - For simple local development, the default SQLite `DATABASE_URL` is usually fine.
#    - For PostgreSQL, ensure you have a running PostgreSQL server and update the
#      `DATABASE_URL` accordingly.
# 3. The `.env` file should NOT be committed to version control if it contains secrets.
#    Ensure `.env` is listed in your `.gitignore` file.
# ==============================================================================
